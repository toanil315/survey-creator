import { makeResetStyles } from '@fluentui/react-components';

export const useColBaseStyles = makeResetStyles({
  boxSizing: 'border-box',
  display: 'block',
  position: 'relative',
  marginTop: '0px',
  marginBottom: '0px',
});
