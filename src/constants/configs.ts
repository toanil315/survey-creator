export const ACCESS_TOKEN = 'access_token';
export const REFRESH_TOKEN = 'refresh_token';
export const DEFAULT_LIMIT = 10;

export enum SORT_ORDER_ENUM {
  ASC = 'ascending',
  DESC = 'descending',
}

export enum SERVER_SORT_ORDER_ENUM {
  ascend = 'ASC',
  descend = 'DESC',
}

export enum ROLES_ENUM {
  ADMIN = 'admin',
  USER = 'user',
}
